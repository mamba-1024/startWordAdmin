// 时间格式
export const dateFormat = 'YYYY-MM-DD HH:mm:ss';
export const dateFormat2 = 'HH:mm:ss';

// 判断值是否为空
export const isNull = (value) => {
  return value === null || value === undefined || value === '';
};
